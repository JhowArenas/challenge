import React, { Fragment } from 'react';

import Navbar from './Navbar';

function Header() {
    return (
        <Fragment>
            <Navbar/>
        </Fragment>
    );
  }

export default Header;